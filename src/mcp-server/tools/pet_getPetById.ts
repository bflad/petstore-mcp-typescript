/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { pet_getPetById } from "../../funcs/pet_getPetById.js";
import { GetPetByIdRequest$zodSchema } from "../../models/operations/getpetbyid.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetPetByIdRequest$zodSchema,
};

export const tool$pet_getPetById: ToolDefinition<typeof args> = {
  name: "pet-get-pet-by-id",
  description: `Find pet by ID

Returns a single pet`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await pet_getPetById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
