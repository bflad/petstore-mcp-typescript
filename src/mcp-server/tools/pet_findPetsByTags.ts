/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { pet_findPetsByTags } from "../../funcs/pet_findPetsByTags.js";
import { FindPetsByTagsRequest$zodSchema } from "../../models/operations/findpetsbytags.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: FindPetsByTagsRequest$zodSchema.optional(),
};

export const tool$pet_findPetsByTags: ToolDefinition<typeof args> = {
  name: "pet-find-pets-by-tags",
  description: `Finds Pets by tags

Multiple tags can be provided with comma separated strings. Use tag1, tag2, tag3 for testing.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await pet_findPetsByTags(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
