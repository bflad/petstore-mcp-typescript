/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { pet_updatePet } from "../../funcs/pet_updatePet.js";
import { Pet$zodSchema } from "../../models/components/pet.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: Pet$zodSchema,
};

export const tool$pet_updatePet: ToolDefinition<typeof args> = {
  name: "pet-update-pet",
  description: `Update an existing pet

Update an existing pet by Id`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await pet_updatePet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
